# Dashboard dependencies.
# Lower bounds, consistent with the root requirements.txt -- pinning exact
# versions here that differ from the root set would force a downgrade of the
# agent API's own dependencies on a fresh install.
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
jinja2>=3.1,<4

# Form posts from the review UI.
python-multipart>=0.0.9

# dashboard/api_adapter.py forwards to the agent API over HTTP.
httpx>=0.27
